// Единый источник списка хостов OAuth-провайдеров теперь shared/oauthProviders.js
// (используется и main, и renderer — esbuild's bundle:true резолвит
// относительные require() за пределами renderer/ так же, как обычный Node
// require, см. комментарий в самом shared-модуле).
//
// BUGFIX ("Яндекс открыл браузер для авторизации и умерла сессия там" —
// live-reproduced): этот файл раньше держал СВОЮ отдельную копию списка без
// passport.yandex.ru (Яндекс.Почта логинит именно через него, не через
// oauth.yandex.ru — см. shared/oauthProviders.js), а renderer/webview-tabs-bind.js
// держал ЕЩЁ одну отдельную копию с тем же пробелом. Обе успели разойтись с
// shared/oauthProviders.js, который уже содержал верный список — просто
// ни один вызывающий код так и не был переключён на него. Реэкспортируем
// вместо повторного дублирования, чтобы список было физически невозможно
// снова рассинхронизировать между main и renderer.
module.exports = require('../../shared/oauthProviders')
